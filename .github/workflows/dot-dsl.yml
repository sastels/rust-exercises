name: dot-dsl
on: [push]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        working-directory: ./dot-dsl
        run: cargo build --verbose
      - name: Run tests
        working-directory: ./dot-dsl
        run: cargo test --verbose
